"use client";

import { motion } from "framer-motion";
import { FiBriefcase, FiCpu, FiTarget, FiZap } from "react-icons/fi";

const stats = [
    { value: "10+", label: "Projects built" },
    { value: "3+", label: "Hackathons" },
    { value: "4", label: "Work areas" },
];

const highlights = [
    {
        title: "Product thinking",
        copy: "I start with the user flow, then shape the interface and data around it.",
        icon: FiTarget,
    },
    {
        title: "Wide build range",
        copy: "I work across web apps, backend APIs, AI/ML ideas, mobile apps, and IoT projects.",
        icon: FiCpu,
    },
    {
        title: "Team ready",
        copy: "Hackathons and group projects helped me move fast, communicate clearly, and finish work.",
        icon: FiBriefcase,
    },
];

const journey = [
    {
        year: "IITRAM",
        title: "Computer Science base",
        copy: "Learning programming, web development, software design, and problem solving.",
    },
    {
        year: "Projects",
        title: "Hands-on building",
        copy: "Created dashboards, booking systems, marketplace flows, mobile concepts, and ML prototypes.",
    },
    {
        year: "Now",
        title: "Internship focused",
        copy: "Improving frontend quality, backend structure, DSA, and project presentation.",
    },
];

const About = () => {
    return (
        <section id="about" className="section-shell">
            <div className="section-inner">
                <motion.div
                    initial={{ opacity: 0, y: 24 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    viewport={{ once: true, amount: 0.3 }}
                    transition={{ duration: 0.6 }}
                    className="max-w-3xl"
                >
                    <span className="section-kicker">About</span>
                    <h2 className="section-title mt-5">
                        I build practical products with frontend detail and full-stack thinking.
                    </h2>
                    <p className="section-copy mt-5">
                        I am a Computer Science student at IITRAM. I like building useful apps: clear interfaces,
                        connected APIs, database-backed features, and prototypes that solve real problems.
                    </p>
                </motion.div>

                <div className="mt-10 grid gap-5 lg:grid-cols-[1.08fr_0.92fr]">
                    <motion.div
                        initial={{ opacity: 0, y: 30 }}
                        whileInView={{ opacity: 1, y: 0 }}
                        viewport={{ once: true, amount: 0.2 }}
                        transition={{ duration: 0.6 }}
                        className="glass-panel p-6 sm:p-8"
                    >
                        <div className="flex flex-wrap items-start justify-between gap-6">
                            <div className="max-w-2xl">
                                <p className="text-sm font-bold uppercase tracking-[0.16em] text-teal-200">Who I am</p>
                                <h3 className="mt-3 text-2xl font-bold text-white">
                                    A developer who likes shipping complete flows, not just screens.
                                </h3>
                                <p className="mt-4 text-sm leading-7 text-slate-300">
                                    My strongest area is frontend development, but I also connect the backend, database,
                                    authentication, and app logic needed to make the product work end to end.
                                </p>
                            </div>

                            <div className="rounded-xl border border-white/10 bg-white/[0.04] p-5 sm:max-w-sm">
                                <p className="flex items-center gap-2 text-sm font-bold text-amber-200">
                                    <FiZap className="text-amber-300" />
                                    Current focus
                                </p>
                                <ul className="mt-4 space-y-3 text-sm leading-6 text-slate-300">
                                    <li>Responsive UI that works well on mobile and desktop</li>
                                    <li>Cleaner APIs, database flows, and reusable components</li>
                                    <li>DSA practice plus AI/ML and IoT project experiments</li>
                                </ul>
                            </div>
                        </div>

                        <div className="mt-6 grid gap-3 sm:grid-cols-3">
                            {stats.map((stat) => (
                                <div key={stat.label} className="rounded-lg border border-white/10 bg-slate-950/40 p-4 text-center">
                                    <p className="text-2xl font-black text-white">{stat.value}</p>
                                    <p className="mt-1 text-xs font-bold uppercase tracking-[0.14em] text-slate-500">{stat.label}</p>
                                </div>
                            ))}
                        </div>
                    </motion.div>

                    <motion.div
                        initial={{ opacity: 0, y: 30 }}
                        whileInView={{ opacity: 1, y: 0 }}
                        viewport={{ once: true, amount: 0.2 }}
                        transition={{ duration: 0.65, delay: 0.08 }}
                        className="glass-panel p-6 sm:p-8"
                    >
                        <p className="text-sm font-bold uppercase tracking-[0.16em] text-teal-200">Journey</p>
                        <div className="mt-6 space-y-4">
                            {journey.map((item) => (
                                <div key={item.year} className="relative rounded-lg border border-white/10 bg-white/[0.04] p-5 pl-8">
                                    <span className="absolute left-4 top-6 h-2.5 w-2.5 rounded-full bg-teal-300" />
                                    <div className="flex flex-wrap items-center justify-between gap-3">
                                        <h3 className="text-lg font-bold text-white">{item.title}</h3>
                                        <span className="rounded-full border border-amber-300/20 bg-amber-300/10 px-3 py-1 text-xs font-bold text-amber-100">
                                            {item.year}
                                        </span>
                                    </div>
                                    <p className="mt-2 text-sm leading-6 text-slate-400">{item.copy}</p>
                                </div>
                            ))}
                        </div>
                    </motion.div>
                </div>

                <div className="mt-5 grid gap-4 md:grid-cols-3">
                    {highlights.map((item, index) => {
                        const Icon = item.icon;
                        return (
                            <motion.div
                                key={item.title}
                                initial={{ opacity: 0, y: 28 }}
                                whileInView={{ opacity: 1, y: 0 }}
                                viewport={{ once: true, amount: 0.2 }}
                                transition={{ duration: 0.55, delay: index * 0.08 }}
                                className="glass-panel p-6 hover:border-teal-300/40"
                            >
                                <div className="flex h-11 w-11 items-center justify-center rounded-lg bg-teal-300/10 text-teal-200">
                                    <Icon className="text-xl" />
                                </div>
                                <h3 className="mt-4 text-lg font-bold text-white">{item.title}</h3>
                                <p className="mt-2 text-sm leading-6 text-slate-400">{item.copy}</p>
                            </motion.div>
                        );
                    })}
                </div>
            </div>
        </section>
    );
};

export default About;
